# Core app
streamlit==1.50.0
pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
kaleido==0.2.1

# Parquet I/O
pyarrow==16.1.0

# Hugging Face downloads
huggingface_hub==0.24.6

# Country names (nice-to-have but used in code paths)
pycountry>=23.12.11

# Geo stack (for reading ADM1 GeoJSONs with GeoPandas)
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyproj>=3.6

# Plotly click capture used on Home and dashboards
streamlit-plotly-events==0.0.6
requests




